import { assert } from "chai";
import {GameState} from "../gameLogic/GameState";

let testState = new GameState('123456');
testState.ai.hand

describe('placeActive', function () {
   it('Should select a benched pokemon before ')
});